# Raw documentary inventory

`inventory_raw.csv` will contain every item observed in the official `PRIMARY_TOC`. The received BibTeX and PDF package cannot create membership, so the CSV remains header-only until primary evidence is acquired.
