# Result of v1_7

Screening was executed once, under this version. `custom_automated_search_collection.csv` is the
worked list it produced: 986 records from the unified starting list in `../source/`, carried through
pre-pass D and gates A, B and C, with 986 recorded outcomes. `screening_decision_log.csv` is the
decision trail, and the two dated `custom_automated_search_collection_*` sheets are earlier states
of the same worked list.

This folder is the only place in the repository where that result was produced. Later versions
inherit it by reference and do not restate it.
