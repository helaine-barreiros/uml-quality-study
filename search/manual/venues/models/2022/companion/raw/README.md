# Raw documentary inventory

`inventory_raw.csv` must contain every item from the official publisher `PRIMARY_TOC`, without relevance filtering. It remains header-only because publisher access is blocked. Raw values must be immutable after registration.
