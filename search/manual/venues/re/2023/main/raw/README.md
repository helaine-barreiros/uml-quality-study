# Raw documentary inventory

The controlled IEEE publisher TOC contains 63 ordered items and passed offline completeness checks. Publication of raw membership rows is deferred because the required official `VENUE_CROSSCHECK` is absent. `inventory_raw.csv` therefore remains header-only. BibTeX does not define membership.
