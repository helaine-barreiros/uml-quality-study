One-shot mutators that ran under this version, kept frozen as the record of how a state was
reached. They are never repointed when folders move: a mutator that no longer names the paths it
actually wrote to has stopped being evidence.

Empty because no mutator has run under v3_0 yet.
